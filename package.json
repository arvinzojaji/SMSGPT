{
  "name": "smsgpt",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "openai": "^4.55.7",
    "twilio": "5.0.3"
  },
  "devDependencies": {
    "twilio-run": "^4.0.3"
  },
  "engines": {
    "node": "18"
  }
}
